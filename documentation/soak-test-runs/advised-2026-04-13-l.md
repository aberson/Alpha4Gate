# Advised Improvement Run: 20260413-2334

## Configuration

- **Command:** `/improve-bot-advised --mode both --games 3 --hours 1 --difficulty 5`
- **Effective mode:** `training`
- **Strategy:** seventh consecutive run. Eval queue from run 2310 was still blocking a new daemon soak, so pivoted to expanding diff-5 hybrid validation sample.
- **Stopped early by user:** "I think we have some wins, let's stop the validation"

## Baseline

- **Git SHA:** 587a3a2 (post breakthrough run 2310)
- **Tag:** `advised/run/20260413-2334/baseline`
- **Starting v1.zip:** the diff-4-trained breakthrough checkpoint

---

## What happened

### Attempted daemon soak: blocked

```
GET /api/training/triggers → "reason": "training already in progress"
```

Prior run's eval queue (only 4 of ~20 games completed before early stop) was still blocking. Same pattern as the gap between runs 2229 and 2310.

### Pivoted to diff-5 validation expansion

Started a 5-game loop at diff 5 hybrid to confirm the 1/2 breakthrough win rate from run 2310. User stopped the run mid-game 1 before any validation result recorded.

### Meanwhile, the blocked eval queue kept running in the background

While the validation was starting, 4 more diff-4 eval games completed:

| Game | Result |
|------|--------|
| eval_v1_74b9f874 | **WIN** (12:54) |
| eval_v1_c9af1ebb | **WIN** (15:56) |
| eval_v1_2699eaa1 | **WIN** (8:45) |
| eval_v1_670f02ba | LOSS (8:23) |

**Cumulative diff-4 eval: 6W-2L (75%) over 8 games.** The 3/4 signal from run 2310 was not variance — the diff-4-trained checkpoint reliably beats the diff-4 reference 3 out of 4 times.

---

## Cumulative tally across runs 2310 + 2334

| Category | Record | Source |
|----------|--------|--------|
| Diff 3 eval (cumulative, new v1) | 11/11 (100%) | Runs 2159 + 2229 backlog |
| Diff 4 eval (cumulative) | **6/8 (75%)** | Run 2310 + run 2334 backlog |
| Diff 5 hybrid | 1/2 (50%) | Run 2310 |

The diff 5 hybrid sample is still n=2 (one win, one loss; one validation crash) — too small for a confident win-rate estimate, but the direction is positive.

---

# Morning Report

## Summary

- **Duration:** ~5m (user-stopped early)
- **Iterations attempted:** 0 new (daemon blocked)
- **Background evaluation progressed:** +4 diff-4 eval games, confirming 75% win rate
- **Stop reason:** user stopped validation; eval queue block made new training infeasible

## Metrics

| Metric | After run 2310 | After run 2334 | Delta |
|--------|----------------|----------------|-------|
| Diff-4 eval wins | 3/4 (75%) | 6/8 (75%) | +3 wins sustained at same rate |
| Diff-5 hybrid wins | 1/2 | 1/2 | no new samples |

## Iteration Log

| # | Attempt | Result | Reason |
|---|---------|--------|--------|
| 1 | Daemon soak @ diff 5 | **BLOCKED** | Eval queue from run 2310 still held "training in progress" flag |
| 2 | Expand diff-5 validation (n=5) | **USER-STOPPED** | User said "we have some wins" — stopped mid-val-1 |

## Findings

1. **Diff-4 performance confirmed.** 6/8 (75%) over 8 eval games is a strong, non-fluke signal. The diff-4-trained checkpoint decisively beats the diff-4 reference.
2. **Eval queue re-blocked.** Run 2310 stopped eval at 4 games, but the daemon continued feeding more eval games whenever backend was alive. These finished in background over the next ~20 min, still flagging the daemon as busy. This reinforces the `feedback_daemon_eval_queue_persists.md` note: even early-stopped eval keeps running across daemon lifecycles.
3. **Background eval is useful data.** The queue-continuation is a gotcha for starting new training, but it does generate free validation samples. Run 2334 got +4 diff-4 eval games without doing anything.

## Suggested Next Actions

**Priority 1:** wait for eval queue to fully drain, then do the direct diff-5 soak:
```bash
# Check: curl -s http://localhost:8765/api/training/triggers — should say would_trigger: true
curl -X PUT http://localhost:8765/api/training/daemon/config -H "Content-Type: application/json" \
  -d '{"cycles_per_run":1, "games_per_cycle":3, "current_difficulty":5, "min_transitions":100, "min_hours_since_last":0}'
curl -X POST http://localhost:8765/api/training/start -H "Content-Type: application/json" -d '{}'
```

**Priority 2:** expand diff-5 hybrid validation sample to n≥10 once the daemon is free. Current n=2 with 1 crash is too noisy to quote.

**Priority 3:** still `--self-improve-code` for [bot.py:872](src/alpha4gate/bot.py#L872) — even a perfect PPO has to work around that primitive bug.

**Priority 4:** add a daemon config option for `max_eval_games` so the eval batch can be capped below the default 20 — eliminates the recurring queue-block problem.

## Revert

Nothing to revert — no config edits, no checkpoint changes.

## Tags

```bash
git tag -l "advised/run/20260413-2334/*"
git log advised/run/20260413-2334/baseline..master --oneline
```
