# Soak run — 2026-04-10

- Start time (T0): 13:17:43 local (backend launch per `Training daemon started (interval=60s)` log line)
- Stop time: ~14:44 local — operator-initiated `Ctrl+C` on backend after 10 minutes of daemon-idle. Total wall-clock duration: **~1h 27m** (not the 4h default).
- Stop reason: **early-stop on N=1 promotions** (Section 2.4 explicitly allows this). The loop completed one full cycle end-to-end at 14:32:57 with v5 promoted. After that the daemon went idle awaiting either +500 transitions (impossible while idle) or 1.0h since last_run. Operator chose to stop rather than wait ~50 more minutes for a deterministic time-based trigger that would have repeated the same pattern.
- Stop condition chosen: 4h wall-clock unattended (default)
- Starting `data/` state: Option B — kept current `data/` (post-smoke-4 state, with `daemon_config.json` deleted to restore 5×10 defaults). Pre-soak baseline preserved at `data-pre-soak-2026-04-10/`. Note: 4,852 transitions inherited from smoke 4 already in `training.db`, well past the 500-transition trigger, so the daemon fired its first training cycle immediately on startup rather than waiting in `idle`.
- Daemon config effective values (DaemonConfig dataclass defaults — `data/daemon_config.json` was deleted before this run):
  - check_interval_seconds: 60
  - min_transitions: 500
  - min_hours_since_last: 1.0
  - cycles_per_run: 5
  - games_per_cycle: 10
  - current_difficulty: 1
  - max_difficulty: 10
  - win_rate_threshold: 0.8
  - (no overrides — file deleted before run, no POST to `/api/training/daemon/config`)
- Backend command: `uv run python -m alpha4gate.runner --serve --daemon 2>&1 | tee logs/soak-2026-04-10-backend.log`
- Baseline Alerts tab state at T0: **empty** ("No active alerts." — confirmed by operator screenshot, tabs visible: All / Errors / Warnings / Info, all empty). Captured ~14:30 local; treating as T0 baseline since the dashboard had not been refreshed since launch.
- Effective `current_difficulty` observed in dashboard at T0: **6** (not the dataclass default of 1 — see Finding #1)

## Pre-run state (recorded before T0)

- Branch: master @ a9bc500 (clean except for gitignored `data-pre-soak-2026-04-10/`)
- Tests: 695 Python passing, 105 frontend passing, mypy 0, ruff clean
- Checkpoints: `data/checkpoints/v1.zip` is the fresh 17-dim model from smoke 4 (490 KB). `data/checkpoints/manifest.json` has `best: null` — daemon's first promotion will populate it.
- DB: `data/training.db` migrated 38 → 42 columns (cannon_count, battery_count, next_cannon_count, next_battery_count) during smoke 4.

## Timeline

One line per observation, newest at the bottom. Format: `HH:MM — <what happened>`.

| Timestamp | Note |
|---|---|
| 13:17:43 | Backend launched with `--serve --daemon`. Daemon started, uvicorn bound to :8765, no tracebacks. (Terminal 1 screenshot in `screenshots/terminal1-backend-launch.png` if saved.) |
| 13:19:30 | Frontend (vite v8.0.3) ready on :3000. "Re-optimizing dependencies" notice from vite — first run after a config change, expected. |
| ~13:19 | Live tab screenshot shows game already at `late_game / step 8272 / 6:09 game time / 35-159 supply / Forge×2 / PhotonCannon×1`. Confirmed the daemon started a training game within ~2 minutes of launch. |
| ~13:20 | Loop tab T0 screenshot captured: state=TRAINING, runs_completed=0, transitions_since_last=4852, current_difficulty=**6** (unexpected — see Finding #1), Would Trigger=NO ("training already in progress" — safety gate working). |
| 13:37 | `data/checkpoints/v5.zip` mtime — last checkpoint file modified. `data/daemon_config.json` recreated by daemon at this time with `current_difficulty: 6` (resolves Finding #1). |
| 13:59:48 | Smoke-test run of `scripts/soak_poll.py` against live backend. Captured 1 row before Ctrl+C. Output shape verified — daemon/triggers/curriculum/checkpoints/promotions/stats endpoints all reachable. Data revealed Findings #3, #4, #5. Smoke-test JSONL discarded after inspection; production poll begins fresh in soak-artifacts dir. |
| 14:12:34 | `soak_poll.py` launched in terminal 3, writing to `~/soak-artifacts/2026-04-10/daemon-state.jsonl`. First poll: state=training, runs=0, transitions=6852, difficulty=6, checkpoints=6, promotions=0. |
| 14:14:59 | Poll #3: state=training, transitions=7000. Net +148 transitions over ~2.5 min ⇒ ~1 transition/sec, consistent with one SC2 game running near-realtime. **Daemon has been in `state=training` for ~57 minutes** since backend launch — longest training window observed. Not stuck (transitions accumulating), but exceptional and worth tracking. |
| 14:21:07.123 | `sc2.sc2process.kill_all` cleaned up 29 SC2 processes. Eval-game cleanup between games. |
| 14:21:08.127 | **CRASH**: `alpha4gate.learning.environment` ERROR — game thread crashed in `_sync_game` at `database.py:164` calling `store_game()`. Exception: `sqlite3.ProgrammingError: SQLite objects created in a thread can only be used in that same thread. The object was created in thread id 118356 and this is thread id 112028.` See Finding #6 (BLOCKER). |
| 14:21:08.131 | `alpha4gate.learning.evaluator` INFO — `Eval game 19/20 (checkpoint=v5, difficulty=6)`. **REVELATION**: daemon was NOT in training-cycles phase; it has been running the 20-game promotion gate evaluation of v5 for some time. The dashboard `state=training` covers BOTH PPO update phase AND eval phase. See Finding #8. |
| 14:21:13.825 | New SC2 game launched: Map=Simple64, Bot=`_FullTrainingBot(Protoss)`, Computer=`VeryHard(Random,RandomBuild)`. Difficulty 6→VeryHard mapping confirmed correct (`environment.py:215`). The eval continued straight past the crashed game with no apparent error handling at the eval level — see Finding #7 (BLOCKER). |
| 14:21:19.651 | New game entered `Status.in_game`. Eval is now running game 20 of 20 (or possibly game 19 retry — exact attribution unclear from log alone). |
| 14:09 → 14:21 | **Crashes #1-5** (lines 1156, 2400, 3460, 4182, 4252) — five sequential `sqlite3.ProgrammingError` game-thread crashes during eval games 14, 15, 16, 17, 18. Each followed by `Eval game N/20` line; eval continued past every one. Same root cause as Finding #6. |
| 14:28:37 → 14:32:00+ | **Game timeout spam loop** — backend log lines 6522 through 9210+ contain ~200 `INFO Game timeout at NNNs ▒ sending terminal observation` entries across ~3.5 minutes wall-clock. In-game time progresses 904s → 1171s. Once a game hits the timeout threshold, the warning re-fires on every game step instead of once. See Finding #15. |
| 14:29:07.217 | `Eval game 20/20 (checkpoint=v5, difficulty=6)` — final eval game begins. |
| 14:32:55.912 | `kill_switch: Process cleanup for 31 processes` — game 20 ended (timeout). |
| 14:32:57.925 | **Crash #6** — game 19 (or 20) result fails to record. SQLite ProgrammingError. |
| 14:32:57.967 | **PROMOTION FIRED**: `Promoted checkpoint to best: v5 (previous: None)`. Gate did NOT execute its quality-comparison code path because there was no previous best — bootstrap promotion. |
| 14:32:57.968 | `Promotion decision: v5 (promoted=True, reason=no previous best checkpoint)`. |
| 14:32:57.997 | `Daemon: training complete -- cycles_completed=5, total_games=50, final_difficulty=6, cycle_results=[v1@d2/0.875, v2@d3/0.875, v3@d4/0.875, v4@d5/0.875, v5@d6/0.875]`. **Section 4.2 six-step success criterion now met.** |
| 14:32:58.067 | **Crash #7** — different traceback (`aiohttp WSMessageTypeError` from a leftover sc2 protocol thread, then re-raised as game-thread crash). Cleanup of a stale connection. Distinct from the SQLite crashes. |
| 14:33:04 | **Poller poll #18** catches the state transition: `state=idle, runs_completed=1, transitions_since_last=0`. First confirmed end-to-end loop completion. |
| 14:33 → 14:43 | Polls #18-27: daemon idle, no activity. Transitions stay at 0 because no games run when daemon is idle ⇒ transition trigger can't fire ⇒ next cycle requires the time-based trigger (~14:32 + 1h = ~15:32 earliest). Daemon is effectively deadlocked until the time trigger. See Finding #12. |
| ~14:44 | Operator stopped the backend in terminal 1 (`Ctrl+C`). Bash prompt appears at backend log line 10043. |
| 14:45:07 | Poller poll #28: all fields `?` — backend not responding. |
| 14:46:31 | Poller poll #29: same. Operator stopped poller after this. |

## Alerts fired

| Timestamp | Severity | Rule | Accurate? (y/n/unknown) | Notes |
|---|---|---|---|---|

## Daemon state transitions

| Timestamp | State | Notes |
|---|---|---|

## Findings

| # | Description | Severity | Category | Evidence |
|---|---|---|---|---|
| 1 | RESOLVED (was: "current_difficulty: 6 despite deleted config"). Actual story: daemon started at difficulty 1 per dataclass defaults, fired training cycle immediately on the 4,852 leftover transitions, ran 5 cycles back-to-back, each advanced curriculum (win_rate 0.875 > threshold 0.8), final state = difficulty 6. Daemon then persisted via `save_daemon_config()` which recreated `data/daemon_config.json` at 13:37 with `current_difficulty: 6`. Not a bug. | n/a (resolved) | daemon | `daemon-state.jsonl` first row + `data/checkpoints/manifest.json` showing 5 new checkpoint cycles + `data/daemon_config.json` mtime 13:37 |
| 2 | At T0 the Loop tab `Next Check` field displays the backend startup time `1:17:43 PM` and has not advanced — the check-loop timer appears paused while training is in progress. Verify after first cycle completes whether it advances correctly. | minor (verify, may be expected) | dashboard | Loop tab T0 screenshot |
| 3 | `save_checkpoint()` at `src/alpha4gate/learning/checkpoints.py:63` blindly appends to `manifest["checkpoints"]` without deduping by name. After this session's first cycle, `manifest.json` contains two entries both named `v1` with different metadata (smoke 4's `v1` and the current session's first `v1`). Manifest will keep growing duplicate rows on every restart that overwrites an existing checkpoint name. | major | training | `data/checkpoints/manifest.json` after first poll at 13:59:48; first poll row in `daemon-state.jsonl` shows two entries with `name: "v1"` |
| 4 | REVISED AGAIN. Initial reading was "50 games completed in <20 min" — wrong, that misread checkpoint metadata as session activity. Second reading was "still in cycle 1 of run 1 after ~57 min" — also wrong, mistook eval phase for training phase. ACTUAL story (per backend log at 14:21:08): the daemon completed its training cycles relatively quickly (smoke 4's checkpoints were already at v5/difficulty=6, so curriculum was already advanced), then entered the **20-game promotion gate evaluation** of v5 around 13:37, which has been running for ~45 min and is currently at game 19/20. The daemon-status `state=training` field encompasses BOTH the PPO training phase AND the eval phase, so the dashboard and poller could not distinguish them — see Finding #8. The original "checkpoint mtimes 13:21-13:37" mystery is now likely explained by the training cycle phase running for those ~20 minutes after backend startup. | n/a (resolved into Findings #6/#7/#8) | training | Backend log line 14:21:08.131 `Eval game 19/20 (checkpoint=v5, difficulty=6)` is the load-bearing observation |
| 5 | The soak procedure (`documentation/soak-test.md` Section 2.1) implies that deleting `data/daemon_config.json` before a soak resets daemon state to dataclass defaults. In practice the daemon recreates the file via `save_daemon_config()` after the first curriculum advancement, so deleting it doesn't actually pin the run to the defaults — it just defers the persisted state by one cycle. Document this gap so future soak operators don't expect a "clean" reset. | minor | docs | This run's `daemon_config.json` mtime 13:37 with `current_difficulty: 6` despite pre-soak deletion |
| 6 | **BLOCKER — SQLite thread-safety crash in `TrainingDB`.** `src/alpha4gate/learning/database.py:127` creates the connection with `sqlite3.connect(str(self._path))` — no `check_same_thread=False`. The Python sqlite3 module pins each connection to its creating thread by default; any cross-thread use raises `sqlite3.ProgrammingError`. At 14:21:08.127 a game-thread call to `store_game()` crashed with exactly this error (creating thread 118356, calling thread 112028). At least one eval game's record was lost; the promotion decision is about to be made on that incomplete dataset. Fix: pass `check_same_thread=False` AND wrap writes in a lock, OR give each thread its own connection, OR funnel writes through a single writer thread. | **blocker** | daemon / database | Backend log traceback at 14:21:08.127; code at `database.py:127`; entire game record loss path in `environment.py:_sync_game` |
| 7 | **BLOCKER — Silent eval-game crash recovery (same antipattern as F2 from earlier today).** The game thread crashed at 14:21:08.127 with the SQLite error from Finding #6. By 14:21:13.825 — five seconds later — a brand new game was launched and the eval kept going. Nothing in the eval-level loop appears to surface the crash as a failed game; the loop catches/logs the exception and treats the game as if it had completed. The promotion gate decision is therefore being made on what the eval BELIEVES are 20 valid game results, when at least one is corrupt or missing. This is the same shape as F2 from earlier today (silent exception fallthrough in trainer loop); the lesson did not propagate to the eval loop. | **blocker** | daemon / evaluator | Backend log timeline: crash at 14:21:08, new `Creating new game` at 14:21:13, no `Eval failed` / `eval game N error` line in between |
| 8 | **MAJOR — Daemon `state` field is too coarse: `state=training` covers both PPO update phase AND 20-game promotion eval phase.** From the dashboard or `/api/training/daemon` endpoint there is no way for the operator to tell whether the daemon is currently doing training cycles or running the eval gate. We spent ~57 minutes thinking the daemon was still in training cycles when it was actually in eval. Suggest: split `state` into distinct values like `training_cycle`, `evaluating`, `promoting`, `idle`. | major | dashboard / daemon | Dashboard Loop tab at T0 says `TRAINING`; poller `daemon.state` field returns `"training"` consistently; backend log reveals true state is "evaluator running game 19/20" |
| 9 | **MINOR — Curriculum range mismatch.** `environment.py:209-219` maps `current_difficulty` 1-10 to SC2 `Difficulty` enum, but **9 → CheatInsane** and **10 → CheatInsane** are both the same opponent. The curriculum supports 10 levels but only 9 are actually distinct. Either tighten `max_difficulty` default to 9, or add a real 10th opponent, or document that 9-10 are intentional duplicates. | minor | training | `src/alpha4gate/learning/environment.py:218-219` |
| 10 | **BLOCKER (severity multiplier on #6) — Crash rate is 30% of eval games, not occasional.** The full backend log shows 6 SQLite ProgrammingError crashes during the 20-game eval phase (games 14-20 all crashed at the store_game step), plus a 7th unrelated `aiohttp WSMessageTypeError` crash on a leftover sc2 protocol thread. That's 30% of eval games losing their result records, on a code path that the daemon hits every cycle. Finding #6 is not "a bug we observed once" — it's "every eval game crashes at completion under current conditions." Fix is the same as #6 (`check_same_thread=False` + locking). | **blocker** | daemon / database | Backend log lines 1156, 2400, 3460, 4182, 4252, 9686, 9719 — seven distinct tracebacks |
| 11 | **MAJOR — First-cycle promotion is unconditional, so the soak did NOT exercise the gate's comparison logic.** The promotion at 14:32:57 fired with `reason=no previous best checkpoint`, which short-circuits the win-rate comparison entirely. Any checkpoint passed to the gate when `manifest.best == null` will promote. This means the 6 corrupted/missing eval game records (Finding #10) did not affect THIS promotion — but they WOULD affect a second-cycle promotion where the gate has to compare new vs best. To validate the gating logic we need a follow-up soak that starts with `manifest.json` already pointing at a best (e.g., copy v5 → manifest.best before launch). | major | daemon / promotion | Backend log line 9714: `Promotion decision: v5 (promoted=True, reason=no previous best checkpoint)` |
| 12 | **MAJOR — Daemon deadlocks on idle: no transitions arrive while no games run.** After run #1 completed, daemon went `state=idle, transitions_since_last=0`. The transition trigger (`min_transitions=500`) can never fire while idle because nothing produces transitions. The only escape is the time trigger (`min_hours_since_last=1.0`). For a 4-hour soak running cleanly, that's a hard ceiling of ~3-4 cycles maximum. For an unattended 24-hour soak, this dominates the cycle rate. Either the daemon should run a low-rate "background self-play" stream while idle to keep transitions flowing, or the time trigger should be the primary trigger, or the operator should accept that "idle" really means "wait for the clock." Document explicitly. | major | daemon / docs | Poller polls #18-#27 in `daemon-state.jsonl` show `state=idle, transitions=0` for 10 minutes with no activity |
| 13 | **MINOR — Poller pollutes the backend log.** ~~`soak_poll.py` hits 6 endpoints every 60s; uvicorn logs each as a separate `INFO: 127.0.0.1:NNNNN - "GET /api/... HTTP/1.1" 200 OK` line in stdout (and therefore the tee'd backend log). Of the 10,044 lines in this run's backend log, the vast majority are these access-log lines, drowning out real findings. When grepping for triage, always filter `INFO:.*127.0.0.1:` first.~~ Optional fix: configure uvicorn `--log-config` to silence access logs, OR have the poller hit a single combined endpoint, OR run the poller against a separate read-only mirror. | minor | observability / docs | Backend log lines 9844-10040 — every line is a poller-driven uvicorn access log |
| 14 | **MAJOR — Alerts pipeline never fired despite 7 game-thread crashes.** At T0 the Alerts tab was empty ("No active alerts."). After the run completed (with 7 backend exceptions, 6 of them on the same root-cause code path, all logged at ERROR level by `alpha4gate.learning.environment`), the Alerts tab was STILL empty per operator inspection. Either (a) the alert engine isn't subscribed to the `alpha4gate.learning.environment` logger, (b) there's no alert rule configured for `Game thread crashed` / `sqlite3.ProgrammingError`, or (c) the alert pipeline is broken altogether. The operator's procedural instinct ("go look at the Alerts tab") would have shown a clean dashboard during a serious failure. Highest-priority observability finding. | **major** | dashboard / alerts | Operator screenshot of Alerts tab after run; backend log error count at ERROR level vs. zero alerts surfaced |
| 15 | **MINOR — Game-timeout spam loop.** Backend log lines 6522 through 9210+ contain ~200 `Game timeout at NNNs ▒ sending terminal observation` entries spanning ~3.5 wall-clock minutes during a single game. The warning re-fires every game step once the game crosses its timeout threshold instead of firing once. Should be a one-shot log + flag check. | minor | logging / environment | Backend log lines 6522-9274 (in-game time advances 904s → 1171s with one log line per few seconds) |
| 16 | **MAJOR (procedure / docs) — Backend log captured by scrollback copy lost the first 51 minutes.** Backend launched 13:17:43 but `~/soak-artifacts/2026-04-10/backend.log` starts at 14:08:16 — the entire training-cycles phase and first ~14 eval games are missing. Operator copy-pasted from terminal scrollback at end of run (per `soak-test.md` Section 5.2 fallback) and the buffer overflowed. We cannot now tell whether the SQLite bug affects training games or only eval games — a meaningful gap for triage. Section 5.2 needs a stronger requirement: the `tee` command in Section 3.2 must be set up FROM THE START, OR the operator must verify scrollback is set to unlimited / very large before launch. Add this as a hard-required prerequisite in Section 1. | major | docs / procedure | First line of `~/soak-artifacts/2026-04-10/backend.log` shows `2026-04-10 14:08:16` vs T0 of `13:17:43` |
| 17 | **MAJOR (statistical anomaly) — All 5 training cycles report identical 0.875 win rate.** From the `Daemon: training complete` line: cycle 1 (difficulty 2), cycle 2 (difficulty 3), cycle 3 (difficulty 4), cycle 4 (difficulty 5), cycle 5 (difficulty 6) — every single one is exactly `win_rate=0.875`. A model facing progressively harder opponents producing identical win rates 5x in a row is improbable. **`0.875 == 7/8`**, which is what you'd get if `games_per_cycle=10` but only 8 of 10 game records made it to the DB (the SQLite bug eating 2 per cycle), with 7 wins among the 8 stored. That's circumstantial evidence the SQLite bug DID affect training games too, masked by a `wins / db_count` calculation that silently divided by the smaller surviving denominator. Alternative explanation: the win_rate comes from a different in-memory source unaffected by the DB failures, in which case the uniformity is a coincidence — but that needs verification by reading the trainer's win-rate calculation code path. | **major** | training / database | Backend log line 9715 (`cycle_results` block); cross-reference with Finding #6's root cause and Finding #16's blind-spot caveat |

## Screenshot index

Screenshots live at `~/soak-artifacts/2026-04-10/screenshots/`. Reference filenames here when added.

| Checkpoint | Tab | Filename |
|---|---|---|
| T0 | Loop | screenshots/loop-T0.png |
| T0 | Improvements | screenshots/improvements-T0.png |
| T0 | Alerts | screenshots/alerts-T0.png |
| T0 | Training | screenshots/training-T0.png |
| T0 | Stats | screenshots/stats-T0.png |
| Tmid | Loop | screenshots/loop-Tmid.png |
| Tmid | Improvements | screenshots/improvements-Tmid.png |
| Tmid | Alerts | screenshots/alerts-Tmid.png |
| Tmid | Training | screenshots/training-Tmid.png |
| Tmid | Stats | screenshots/stats-Tmid.png |
| Tfinal | Loop | screenshots/loop-Tfinal.png |
| Tfinal | Improvements | screenshots/improvements-Tfinal.png |
| Tfinal | Alerts | screenshots/alerts-Tfinal.png |
| Tfinal | Training | screenshots/training-Tfinal.png |
| Tfinal | Stats | screenshots/stats-Tfinal.png |
