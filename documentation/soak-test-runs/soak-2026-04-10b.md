# Soak run — 2026-04-10 (re-soak after blocker fixes #66/#67/#68)

- Start time (T0): 2026-04-10 19:26:50 local (backend PID 105480; Uvicorn on :8765; daemon auto-started via --daemon flag)
- Stop condition chosen: 4h wall-clock unattended
- Starting `data/` state: Option A — restored from `data-pre-soak-2026-04-10/` (clean baseline; soak-1's tainted v2-v5 discarded)
- Daemon config effective values: DaemonConfig dataclass defaults (no `data/daemon_config.json` in restored snapshot — verify after launch)
  - check_interval_seconds: 60
  - min_transitions: 500
  - min_hours_since_last: 1.0
  - cycles_per_run: 5
  - games_per_cycle: 10
  - current_difficulty: 1
  - max_difficulty: 10
  - win_rate_threshold: 0.8
- Backend command: `DEBUG_ENDPOINTS=1 uv run python -m alpha4gate.runner --serve --daemon 2>&1 | tee logs/soak-2026-04-10b-backend.log`
- Baseline Alerts tab state at T0: <fill in after dashboard load>
- Master tip at launch: b9fc44c (Phase 4.5 Step 5 blocker fixes #66/#67/#68 + soak_poll UP017 ruff fix)

## Pre-flight checklist results

- [x] manifest.best non-null at T0 — verified `"best": "v1"` pre-launch (data/ restored from data-pre-soak-2026-04-10/)
- [x] tee target file exists and stdout is flushing — 7 lines within 5s, daemon log visible (no Python stdout buffering)
- [ ] synthetic alert fires and reaches the Alerts tab within ~5s — **PENDING OPERATOR** (see soak-test.md §3.5)

## Timeline

(Newest at the bottom. `HH:MM — what happened`.)

- 19:26 — data/ restored from data-pre-soak-2026-04-10/ (manifest.best="v1", v1.zip+v2.zip+v3.zip present). Prior data/ had soak-1's tainted best="v5" — discarded per Option A.
- 19:26:50 — backend launched (DEBUG_ENDPOINTS=1, --serve --daemon), tee → logs/soak-2026-04-10b-backend.log, Uvicorn bound to :8765, daemon auto-start confirmed.
- 19:26:55 — tee file sanity-checked: stdout flushing, no Python buffering. Awaiting operator to run the synthetic alert pre-flight from documentation/soak-test.md §3.5 before committing to the 4h unattended window.

## Alerts fired

| Timestamp | Severity | Rule | Accurate? (y/n/unknown) | Notes |
|---|---|---|---|---|

## Daemon state transitions

| Timestamp | State | Notes |
|---|---|---|

## Findings

| Description | Severity | Category | Evidence |
|---|---|---|---|
| **Soak ABORTED at T0+4min.** Restored checkpoints in `data-pre-soak-2026-04-10/` are 15-dim observation space (pre-Phase-4.5-F1); current `SC2Env` is 17-dim. Every cycle of the daemon's first training run crashed at `trainer.py:160` `model.set_env(env)` with `ValueError: Observation spaces do not match: Box(0.0, 1.0, (15,), float32) != Box(0.0, 1.0, (17,), float32)`. Zero games played. Root cause documented in commit afaeda5 ("Same root anti-pattern as F1: the obs space drift 15→17 from old checkpoints"). afaeda5 fixed the producer side but did not migrate saved checkpoints — the 15-dim shape is baked into v1/v2/v3.zip and is unrecoverable. Snapshot Option A is invalid against current master. Only valid path forward is Option B (fresh empty `data/`). | blocker | daemon | `~/soak-artifacts/2026-04-10b/backend-aborted.log` lines 8-76 (full 5-cycle crash trace + daemon "training complete cycles_completed=5 total_games=0" line) |
| **#69 (run_job all-crashed status) is now load-bearing.** Daemon logged `training complete -- {cycles_completed: 5, total_games: 0, ...}` after all 5 cycles crashed with `status: crashed`. The daemon treats "ran the loop 5 times" as "training complete" regardless of per-cycle status. Any future soak with training-side crashes will report success in `/api/training/daemon` state/history while the backend log carries the stack traces. #69 was filed as a follow-up after #68 closed; it must be fixed before or during soak-2 prep or soak-2 observability is invalid. | blocker | daemon | backend-aborted.log line 76 |

## Positive signals captured before abort
- **#68 (alerts pipeline) verified working.** Synthetic error via `POST /api/debug/raise_error` reached `/api/training/status.recent_errors` at `02:30:33 UTC` with correct message. Pre-flight §3.5 pipeline is alive. Dashboard visual confirmation not attempted (frontend never started).
- **Python stdout flushing verified.** Daemon start log line appeared in tee file within <5s of launch. `feedback_python_stdout_buffering` fix is working in the soak path.
- **tee capture verified.** 173 lines captured end-to-end through abort. Backend log saved to artifact dir before `data/` is touched again.
- **§5.1 Option C kill path verified.** `cmd.exe //c "taskkill /T /F /PID <pid>"` cleanly terminated backend tree on first try.

## Abort decision
- Stopped at 19:33 local (T0 + ~7 min). Backend killed via taskkill. `data/` left in restored-but-broken state pending operator decision between Option B and fixing #69 first.

