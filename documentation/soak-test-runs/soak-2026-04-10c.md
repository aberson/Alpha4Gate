# Soak run — 2026-04-10 attempt 2 (after #71 fix)

**Attempt 1 aborted:** see [soak-2026-04-10b.md](soak-2026-04-10b.md). Every training cycle crashed at `trainer.py:160` against restored 15-dim checkpoints vs current 17-dim `SC2Env`. Daemon reported all-crashed as success — that was #71, now fixed in commit `3a7ce10` via `/build-step --reviewers code` gauntlet (zero high findings, one medium, ~9 low; 728 pytest passed; mypy + ruff clean).

- Start time (T0): 2026-04-10 19:59:27 local / 2026-04-11 02:59:27 UTC (backend PID 115004; Uvicorn on :8765; daemon auto-started via --daemon flag; vite frontend on :3000; WebSocket clients auto-reconnected from prior browser tab)
- Stop condition chosen: 4h wall-clock unattended
- Starting `data/` state: **Option B — fresh empty** (no tuning files copied; code defaults apply; `Settings.ensure_dirs()` will recreate the directory at backend startup, daemon/trainer populate subdirectories lazily)
- Daemon config effective values: `DaemonConfig` dataclass defaults (no `data/daemon_config.json` file; defaults from `src/alpha4gate/learning/daemon.py`)
  - check_interval_seconds: 60
  - min_transitions: 500
  - min_hours_since_last: 1.0
  - cycles_per_run: 5
  - games_per_cycle: 10
  - current_difficulty: 1
  - max_difficulty: 10
  - win_rate_threshold: 0.8
- Backend command: `DEBUG_ENDPOINTS=1 uv run python -m alpha4gate.runner --serve --daemon 2>&1 | tee logs/soak-2026-04-10c-backend.log`
- Frontend command: `cd frontend && npm run dev` (vite pinned to :3000 with strictPort)
- Baseline Alerts tab state at T0: operator confirmed clean go at ~20:02 local without flagging leftover alerts — treating baseline as empty. Any alert observed during the soak is attributable to this run.
- Master tip at launch: `3a7ce10` (Phase 4.5 Step 5 #71: daemon reports all-crashed as failure)

## Pre-flight checklist results

- [x] manifest.best — N/A (Option B empty data/; daemon will create `data/checkpoints/` lazily on first training cycle)
- [x] tee target file exists with daemon startup lines — 13+ lines within 8s, daemon log + Uvicorn banner + WebSocket accepts all visible
- [x] backend Uvicorn banner on :8765 confirmed; frontend vite v8.0.3 ready on :3000 in 432ms confirmed
- [x] synthetic alert fires and reaches `/api/training/status.recent_errors` — `{"level": "ERROR", "logger": "alpha4gate.debug", "message": "synthetic error: soak-2c preflight test", "ts": "2026-04-11T02:59:54.946884+00:00"}`. `error_count_since_start=1` (clean — no training-cycle crash contamination like attempt 1 had). **Operator still needs to visually verify the `Backend errors (1)` alert renders on the Alerts tab with severity `error` and persistent (non-auto-dismissing) toast.**
- [x] SC2 at main menu pre-launch (operator confirmed before this attempt)

## Timeline

(Newest at the bottom. `HH:MM — what happened`.)

- 19:59 — data/ wiped to empty (Option B); master at 3a7ce10 (post-#71 fix); ports 8765 + 3000 confirmed free (killed stale vite PID 96672 from a prior session first).
- 19:59:27 — backend launched: DEBUG_ENDPOINTS=1, --serve --daemon, tee → logs/soak-2026-04-10c-backend.log, Uvicorn on :8765, daemon auto-start confirmed, no training trigger fired (0 transitions in fresh DB — expected).
- 19:59:30 — vite frontend launched on :3000, ready in 432ms, browser tab from earlier session auto-reconnected WebSockets.
- 19:59:54 UTC (≈19:59:54 local? — UTC timestamp only from the error buffer) — synthetic alert fired via `POST /api/debug/raise_error`, reached `/api/training/status.recent_errors` within <2s. `error_count_since_start=1`, buffer uncontaminated (contrast attempt 1's 6 errors from 5 bogus training crashes). **Awaiting operator visual confirmation on Alerts tab before committing to the 4h window.**
- ~20:02 — operator visual confirm + "go". Baseline Alerts tab empty.
- ~20:03 — Live tab still empty, no games playing. Investigation: daemon `state: idle, runs_completed: 0, last_error: null`; triggers `would_trigger: false, reason: "no database file"`. **Bootstrapping deadlock identified.** Trigger logic in `daemon.py:185-216` has two safety gates (`not db_path.exists()` and `total_transitions == 0`) that both block a fresh Option B launch. Time trigger at line 240 (`hours_since_last == inf` on a fresh daemon) would fire immediately but the safety gates shortcut before it's evaluated. `--serve --daemon` mode never self-bootstraps without pre-existing transitions. This is an unstated prerequisite in `soak-test.md` §2.1 Option B — a doc gap worth filing as a findings follow-up. **No "force trigger" or "run now" API endpoint or UI button exists** (grep across `src/alpha4gate/` and `frontend/src/` returned no hits). Bootstrap path chosen: run a single seed game in a parallel process via `uv run python -m alpha4gate.runner --map Simple64 --difficulty 1`, tee to `logs/soak-2026-04-10c-seed.log`. Concurrent SQLite access is safe per #66's WAL-mode fix (first time this configuration has been exercised in a soak context; if it fails, finding to capture).
- 20:03 — seed game launched (background task `buqtd3ygg`, tee → `logs/soak-2026-04-10c-seed.log`). Awaiting game completion + first daemon pickup.
- 20:08 — seed game tail check: bot playing normally (game time 0:00 → 4:00+, Claude advisor firing, ~5500 game steps). Bind error `[Errno 10048] error while attempting to bind on address ('0.0.0.0', 8765)` logged — `_run_single_game` calls `_start_server_background` which tries to bind another Uvicorn to 8765 (already held by the soak backend). Error is cosmetic; the game process continues without its own API server and the soak backend's `/api/training/status.error_count_since_start` remains `1` (the seed process's bind error did NOT leak into the backend's ErrorLogBuffer because they're separate processes with separate logger roots). **But:** `data/training.db` still does not exist at T+9min. Investigation of [runner.py:231-289](../../src/alpha4gate/runner.py#L231) `_run_single_game` shows the bot is constructed without `training_db=` (lines 270-277), so `self._training_db = None` inside the bot, and per [bot.py:397](../../src/alpha4gate/bot.py#L397) all transition writes are guarded by `if self._training_db is None: return`. **`--map Simple64` produces zero training data.** The seed path must be `--batch N` — [runner.py:291-390](../../src/alpha4gate/runner.py#L291) `_run_batch` opens `TrainingDB` at line 310, passes it to bot at line 347, calls `bot.record_final_transition` + `db.store_game` at lines 365-374. Docstring on `_run_batch` literally says "*Run N games in sequence, recording transitions for training*." Second unstated prerequisite in soak-test.md §2.1 Option B — the docs don't mention that `_run_single_game` is a no-op for training data while `_run_batch` is the correct seed path.
- 20:10:22 — seed game ended `Result.Victory`, clean SC2 process cleanup via bot's built-in `kill_all`. training.db confirmed still absent (as expected — `_run_single_game` never wrote).
- 20:11 — relaunched with corrected command: `uv run python -m alpha4gate.runner --batch 1 --difficulty 1 2>&1 | tee logs/soak-2026-04-10c-seed-batch.log`. Background task `bdmefg9wm`. This IS the path that writes to `training.db`.
- 20:11 — `data/training.db` created (4096 bytes) shortly after batch game launch. Bot is writing transitions live as it plays, not only at game end.
- 20:12:27 — **Daemon fired first training cycle.** The 20:12 daemon check saw `training.db` + 185 transitions → time trigger fired (`hours_since_last = inf` on a never-run daemon) → daemon state transitioned `idle → training`. Trainer logged `Daemon: starting training (5 cycles, 10 games/cycle, difficulty=1)` followed by `Cycle 1/5 (difficulty=1)` + `Training: 10 games, ~150 timesteps`.
- 20:12:45 — Trainer's own SC2 process launched (`sc2.protocol Status.launched`, then `Status.init_game`, then `Status.in_game`). Player name `_FullTrainingBot(Protoss) vs Easy(Random, RandomBuild)`.
- **20:12:52 → 20:14:17 — Two concurrent SC2 instances coexisted cleanly.** The batch seed game (process `bdmefg9wm`) was still playing its single game while the daemon's trainer cycle 1 launched its own SC2 and started its own game. No SQLite lock contention (#66's WAL mode earning its keep). No SC2 OS-level conflict (burnysc2 launches each game as an independent process tree). No new errors in the backend ErrorLogBuffer (`error_count_since_start` still 1 — only the pre-flight synthetic). **This was an untested configuration heading in; the system handled it without drama.** First positive soak observation.
- 20:14:17 — Batch seed game finished `Result.Victory`, exited cleanly via `sc2.sc2process kill_all`. Final output: `Transitions recorded: 248 -> data\training.db` (so ~248 transitions per 4.5-min game at difficulty 1). Batch seed process exited, only the daemon's trainer cycle 1 now using SC2.
- 20:14+ — Daemon training cycle 1 continues alone. Expected cadence: 5 cycles × 10 games × ~5 min/game ≈ 4–6 hours total for a single training run. That's the full soak window; the daemon should spend most of the 4h inside this first training run. Operator watches Loop / Improvements / Alerts tabs for state transitions, checkpoint appearances, and any alert firing.
- **20:28:24 — Trainer hit a WSMessageTypeError and began mass cleanup.** `aiohttp.client_exceptions.WSMessageTypeError: Received message 257:None is not WSMsgType.BINARY` from `sc2.protocol.__request` on a WebSocket `receive_bytes`. The error propagated up through `sc2.client.step → _play_game_ai → _play_game → _host_game → run_game → environment.py:187 _run_game_thread → environment.py:223 _sync_game`, was caught by `environment.py` which logged `Game thread crashed` via `_log.error(...)`, and `sc2.sc2process.kill_all` fired with `Process cleanup for 22 processes` — revealing that the **RL trainer spawns approximately 10 parallel SC2 instances per cycle via stable-baselines3's VecEnv (roughly 10 environments × 2 tracked process objects each)**. This is an unstated architectural assumption — I had assumed 1 SC2 per game when I predicted "concurrent SC2 works fine" at 20:14, which was wrong.
- 20:28 → 20:33 — Operator observed "many many SC2 copies" in the Windows process list and flagged the issue. `error_count_since_start` climbed to 21, all 21 `recent_errors` identical `Game thread crashed` entries. Daemon `state: training, runs_completed: 0, last_error: null` THE ENTIRE TIME despite 21 logged errors — **#71 fix gap confirmed**: the `_run_training` path in daemon.py only sets `_last_error` after the `orchestrator.run(...)` call *returns*, but the RL orchestrator's VecEnv is still inside its `.learn()` loop retrying failed environments. Per-cycle trainer crashes never reach daemon-level `_last_error` until the whole run exits.
- 20:33 — Abort decision. Investigation parallel: `tasklist` showed only 2 SC2 survivors (PID 114460 from 9:51 AM — 10h orphan, probably from a pre-soak session; PID 114764 from 20:29:36 — trainer straggler). Backend log had 63 matches for crash/cycle/kill_all signals. Plan: stop daemon via API → kill backend via `taskkill /T /F` → kill orphan SC2 → snapshot evidence → record findings → abort.
- 20:34 — `POST /api/training/stop` → `{"status":"stopped"}`, daemon reports `running: False, state: idle`. But this only joined the daemon check thread; the in-flight trainer subprocess was still alive.
- 20:34 — `cmd.exe //c "taskkill /T /F /PID 115004"` terminated backend (PID 115004) and one child (PID 121564). `taskkill /T` correctly walked the backend's subprocess tree but the trainer-spawned SC2 instances were mostly already gone — SC2 process count dropped from 2 → 1. The surviving PID 114464 (trainer straggler) appears to have exited when its parent was killed, leaving only the 10h orphan PID 114460.
- 20:34 — `taskkill /F /PID 114460` killed the orphan. SC2 process count reached 0.
- 20:35 — Evidence preserved to `~/soak-artifacts/2026-04-10c/`: `backend.log` (406KB), `frontend.log` (5KB), `seed-batch.log` (100KB), `seed-map.log` (29KB from the wrong Option A attempt), `training.db` (140KB with 248+ seed transitions and whatever the trainer wrote before crashing). `data/` directory NOT yet zipped — candidate for post-mortem snapshot if the stable-baselines3 VecEnv state or partial reward logs are needed.
- **20:35 — Soak-2 attempt 2 ABORTED. Second aborted attempt of the day. Real findings, not a wasted run.**

## Alerts fired

| Timestamp | Severity | Rule | Accurate? (y/n/unknown) | Notes |
|---|---|---|---|---|
| 2026-04-11T02:59:54Z | error | ruleBackendErrors (synthetic) | n/a (pre-flight) | §3.5 synthetic preflight — dismissed by operator before go. |

## Daemon state transitions

| Timestamp | State | Notes |
|---|---|---|
| 2026-04-10 19:59:27 local | idle (initial) | Backend launch; `running: true, runs_completed: 0` |

## Findings

| Description | Severity | Category | Evidence |
|---|---|---|---|
| **soak-test.md §2.1 Option B does not document the fresh-empty bootstrapping deadlock.** The `--serve --daemon` process cannot fire its first training cycle against an empty `data/` because the trigger logic at `src/alpha4gate/learning/daemon.py:185-216` has two short-circuiting safety gates (`not db_path.exists()` and `total_transitions == 0`) that return before the time trigger (line 240) can evaluate. The daemon has no "run now" API endpoint and no "force trigger" UI button (grep across `src/alpha4gate/` and `frontend/src/` returned no hits). A fresh-context operator following the docs verbatim would launch the backend, open the dashboard, and watch an empty Live tab indefinitely. Correct seed path: `uv run python -m alpha4gate.runner --batch N --difficulty 1` in a parallel process (see sibling finding below). Docs fix: add a "Seed transitions before starting the daemon" step to §2.1 Option B, or build a `POST /api/training/daemon/trigger` endpoint that the dashboard surfaces as a "Run Now" button. | major | docs | This runlog's timeline entry at ~20:03; `src/alpha4gate/learning/daemon.py:185-216` code read; trigger endpoint JSON response with `"reason": "no database file"` |
| **`alpha4gate.runner --map Simple64` (`_run_single_game`) is a silent no-op for training-data purposes.** The bot is constructed without `training_db=` at [runner.py:270-277](../../src/alpha4gate/runner.py#L270), so `self._training_db = None`, and [bot.py:397](../../src/alpha4gate/bot.py#L397) guards all transition writes with `if self._training_db is None: return`. The game plays normally (SC2 attaches, Claude advisor fires, game progresses to a Victory/Defeat result) but `data/training.db` is never created and the daemon continues to sit at `state: idle, reason: no database file`. The correct seed path is `--batch N` — `_run_batch` at [runner.py:291-390](../../src/alpha4gate/runner.py#L291) does open `TrainingDB` and wires it into the bot. This asymmetry is not documented anywhere in `soak-test.md`, the project `CLAUDE.md`, or a comment in `_run_single_game` itself. A sensible seed-game candidate picks the wrong flag and silently wastes 5 minutes. Secondary concern: `_run_single_game` also calls `_start_server_background` which attempts to bind Uvicorn to 8765; when the soak backend already holds that port, the bind error is logged loudly in the seed process's stdout (`[Errno 10048]`) but the game continues without its own API — confusing but not broken. | major | docs / cli ergonomics | `logs/soak-2026-04-10c-seed.log` lines 1-end (4 minutes of game progression ending in `Result.Victory`, zero DB writes); `data/training.db` absent post-game; [runner.py:231-289](../../src/alpha4gate/runner.py#L231) `_run_single_game` bot construction omits `training_db`; [runner.py:291-390](../../src/alpha4gate/runner.py#L291) `_run_batch` shows the correct pattern; [bot.py:397](../../src/alpha4gate/bot.py#L397) transition-write guard |
| **RL trainer's VecEnv crashes with `aiohttp WSMessageTypeError: Received message 257:None is not WSMsgType.BINARY` when daemon training is kicked off with a recently-concurrent external SC2 process.** The full stack: `sc2.protocol.__request:49` `receive_bytes` → aiohttp raises WSMessageTypeError (msg type 257 ≠ BINARY) → re-raised as `sc2.protocol.ConnectionAlreadyClosedError` → caught in `alpha4gate.learning.environment._run_game_thread:187` → logged `Game thread crashed` → trainer cycle crashes. The error fires repeatedly (21 identical `Game thread crashed` entries in `error_count_since_start` over ~15 minutes), suggesting each parallel environment in the VecEnv independently hits the same fault. Seed batch game finished cleanly at 20:14:17, trainer cycle 1 started at 20:12:27 (≈2 minutes of overlap), crash chain began ~14 min after seed exit at 20:28:24 — so the error may be latent ("corrupted socket state persists beyond the concurrent window") rather than directly triggered by the temporal overlap. **The RL trainer spawns approximately 10 parallel SC2 instances per cycle via stable-baselines3 VecEnv (22 processes tracked by `sc2.sc2process.kill_all`).** This is an unstated architectural assumption not mentioned in `soak-test.md`, the CLAUDE.md, or any surrounding comments. | blocker | daemon / rl | `artifacts/2026-04-10c/backend.log` lines ~300-600 (the first stack trace + 21 repeat crashes); `error_count_since_start=21` via `/api/training/status.recent_errors`; `kill_all: Process cleanup for 22 processes` log line |
| **#71 fix has a cycle-granularity gap: per-cycle trainer crashes do not reach daemon-level `_last_error` until the whole training run exits.** The #71 fix at `daemon.py:_run_training` wraps the bookkeeping that runs AFTER `orchestrator.run(...)` returns. But for an RL trainer whose VecEnv is crashing inside `.learn()`, the orchestrator does NOT return — it enters a retry loop inside stable-baselines3 or keeps trying to collect experience from dead environments. The daemon state sits at `state: training, runs_completed: 0, last_error: null` for the entire crash period. Operators watching the dashboard see "healthy training in progress" while 21 errors pile up in the error buffer. The only observable signal is `error_count_since_start` on `/api/training/status` — which the Alerts tab *can* surface via `ruleBackendErrors`, but is not a daemon-state-machine-level signal. **Follow-up candidate: per-cycle or per-crash-batch daemon-level error surfacing, OR a watchdog that fails a training run if `error_count_since_start` climbs by N within one training interval.** | major | daemon observability | daemon state `state: training, last_error: null` observed live during the crash sequence; [src/alpha4gate/learning/daemon.py:517-548](../../src/alpha4gate/learning/daemon.py#L517) — #71 fix only fires on orchestrator return |
| **`_daemon.stop()` via `POST /api/training/stop` only joins the daemon check thread — it does NOT kill the in-flight trainer subprocess.** §5.1 of `soak-test.md` describes Option A (dashboard stop button) as the preferred stop path and says "once it returns, the daemon state should settle to `idle` and `running: false`". That's exactly what happens — but if a training run is in progress when stop is called, the trainer keeps running because it's a separate subprocess (or thread with its own asyncio loop) that `_daemon.stop()` doesn't have a handle on. In this session, Option A returned `{"status":"stopped"}` within ~3 seconds, daemon status went to `state: idle, running: false`, but the trainer was still crashing and spawning SC2 processes in the background. Had to fall back to §5.1 Option C (`cmd.exe //c "taskkill /T /F /PID"`) to actually stop the crash loop. Soak-test.md §5.1 should document that Option A is insufficient mid-training-run and explicitly instruct operators to also kill the backend tree (or to wait for the trainer to finish on its own, which may never happen). | major | docs / daemon stop path | Timeline entries at 20:34 (stop API returned success but trainer kept running); `taskkill` output showing 2 backend children cleaned up AFTER the API stop had already returned "stopped"; `src/alpha4gate/learning/daemon.py:stop()` method scope |
| **Orphan SC2 process from 10 hours before soak launch was still running.** PID 114460 had `StartTime 4/10/2026 9:51:43 AM` and was still alive at 20:33 local — it survived all prior cleanup in the session. Source unknown (possibly pre-soak testing, possibly Battle.net client stale process, possibly soak-1 leftover that never got killed). Its 55MB memory footprint was small enough that the operator didn't notice it before soak-2 started. It was not killed by any trainer `kill_all` (burnysc2 only tracks processes it spawned itself) and did not interact with the soak in any observable way. Still, the persistence of a 10-hour-old SC2 process is a cleanup hygiene finding — soak-test.md §1 prerequisites mentions "Confirm no other SC2 processes are running (Task Manager -> look for stale SC2_x64.exe processes from previous runs and kill them)" but that step is manual and operator-checked. Consider adding a pre-flight script that enumerates SC2 processes and fails if ANY are running. | minor | docs / ops | `powershell Get-Process SC2_x64` output showing PID 114460 StartTime 9:51 AM; `taskkill /F /PID 114460` terminated it successfully |

