# Soak run — 2026-04-11

- Start time (T0): 22:56 local / 05:56 UTC (2026-04-11)
- Stop condition chosen: 4 hours wall-clock unattended runtime
- Starting `data/` state: Option B — fresh empty `data/` (carrying over `hyperparams.json` + `reward_rules.json` from `data-pre-soak-2026-04-11/`; prior `data/` from soak-2 attempts moved aside to `data-pre-soak-2026-04-11/`)
- Daemon config effective values (defaults from `DaemonConfig` dataclass — no `data/daemon_config.json` present):
  - check_interval_seconds: 60
  - min_transitions: 500
  - min_hours_since_last: 1.0
  - cycles_per_run: 5
  - games_per_cycle: 10
  - current_difficulty: 1
  - max_difficulty: 10
  - win_rate_threshold: 0.8
  - (no overrides; no `daemon_config.json` will be created until the daemon first persists state)
- Backend command: `DEBUG_ENDPOINTS=1 uv run python -m alpha4gate.runner --serve --daemon 2>&1 | tee logs/soak-2026-04-11-backend.log`
- Baseline Alerts tab state at T0: empty (fresh `data/`, no pre-existing alerts before §3.5 preflight)
- Side-car observability: `scripts/soak_poll.py` running 60s interval, writing to `~/soak-artifacts/2026-04-11/daemon-state.jsonl`. Backend, frontend, and poller all teed to `logs/soak-2026-04-11-{backend,frontend,poller}.log`.

## Pre-soak context

- Branch: master @ c4165c8 (pushed). mypy 44/44 clean, ruff clean, 742 unit tests + 1 sc2-deselected passing as of last session.
- Phase 4.5 Step 5 blockers #66/#67/#68/#71/#72/#73 all closed. Only #74 (POST /api/training/stop doesn't kill in-flight trainer) remains open as documented nice-to-have; mid-training stop sequence in soak-test.md §5.1.
- #73 watchdog defaults: poll 5s, threshold 5 errors. Interim `daemon.last_error` surfaces within ~25s of the 5th per-cycle ERROR. Healthy run keeps `daemon.last_error == null`.
- Seed plan: `uv run python -m alpha4gate.runner --batch 1 --difficulty 1` (NOT `--map Simple64` — that path drops transitions silently per soak-2 finding §2.1).

## Timeline

One line per observation, newest at the bottom. Format: `HH:MM — <what happened>`.

- 22:44 — Moved soak-2 leftover `data/` aside to `data-pre-soak-2026-04-11/`. Created fresh empty `data/` and copied `hyperparams.json` + `reward_rules.json` over from the snapshot (Option B with carried tuning).
- 22:44 — Seeded `training.db` via `uv run python -m alpha4gate.runner --batch 1 --difficulty 1`. Result: Victory, "Transitions recorded: 134 -> data\\training.db".
- 22:46 — `TrainingDB.get_transition_count()` direct read returns `134`. Seed verified.
- 22:48 — Backend launched with `DEBUG_ENDPOINTS=1 uv run python -m alpha4gate.runner --serve --daemon 2>&1 | tee logs/soak-2026-04-11-backend.log`. §3.2 hard pre-flight line-count gate FAILED (7 lines, gate requires >50). API verification showed daemon healthy (`/api/training/daemon` returned `running:true state:idle last_error:null`, watchdog config wired). Decision: stop and ask user.
- 22:54 — Per user direction, took Option 2: stopped backend, verified port free, relaunched with same command. Health re-verified via API.
- 22:55 — Launched `scripts/soak_poll.py --output ~/soak-artifacts/2026-04-11/daemon-state.jsonl` and `cd frontend && npm run dev` in parallel. Vite ready in 325ms on `:3000`.
- 22:55 — First poller row written. **`triggers.transitions_since_last=511`** despite `TrainingDB.get_transition_count()==134`. Practical effect: transition trigger already met, daemon will fire training on the next 60s check.
- 22:56 — §3.5 synthetic alert preflight: `POST /api/debug/raise_error` returned 200. `/api/training/status` shows `error_count_since_start=1` and the synthetic error in `recent_errors`. Dashboard surfaced `alpha4gate.debug: synthetic error: soak preflight test` — operator confirmed visually. **synthetic preflight: OK.** T0 = 22:56 local.
- 22:56 — Poll #2: daemon transitioned `idle → training` (1st-cycle trigger fired immediately because `transitions_since_last` was already above 500 at backend startup). `cps=1` first checkpoint observed. `last_error=None`.
- 22:58 — Poll #4: still `state=training`, `cps=2` second checkpoint observed within ~4 min of daemon firing. Trainer is cycling cleanly. Operator dashboard screenshot at this time confirms `Daemon State: TRAINING`, `Runs Completed: 0` (in flight), `Transitions Since Last: 791`, `Would Trigger? NO — training already in progress` (safety gate working). Synthetic preflight alert still pinned in bottom-right, undismissed.
- 22:58 — **Resolves earlier finding about `transitions_since_last=511` vs `TrainingDB.get_transition_count()=134`.** It was not a discrepancy — the daemon's first 60s wakeup at 22:55:00 fired training immediately (~1m after backend launch), and by the time the first poll ran at 22:55:17 the trainer's first cycle was already playing games and recording new transitions. The growth 134 → 511 → 559 → 689 → 783 is the trainer's games naturally accumulating experience in the DB. Removing this from the Findings table.
- 23:03 — Cycle progression from backend log: Cycle 1 done 22:57:59 win_rate=0.333 → checkpoint v1; Cycle 2 done 23:00:20 win_rate=0.250 → v2; Cycle 3 done 23:03:30 win_rate=0.200 → v3; Cycle 4/5 in flight. Daemon `state=training`, `runs_completed=0`, `last_error=null`, watchdog quiet, `error_count_since_start=1` (only the synthetic preflight). Healthy loop.
- 23:03 — Operator screenshot of Training tab shows Total Games: 4, Total Transitions: 1047, Checkpoints: 3, Win Rates 25% across all windows. Model Comparison shows rules=1/v1=2/v2=1 = 4. Discrepancy: trainer has actually played 30 games by this time (3 cycles × 10), but only 4 are accounted for in Model Comparison. Filed as Finding 2 below.
- 23:03 — Win rate trajectory: 0.333 → 0.250 → 0.200 (degrading). PPO is producing a worse policy each cycle. None of v1/v2/v3 will hit the `win_rate_threshold=0.8` promotion gate. Filed as Finding 1 below — the soak's most substantive learning-quality finding so far.
- 23:03 — `/api/training/checkpoints` returns two `v1` entries with `cycle=1` but different `win_rate` (0.500 vs 0.333). The 0.500 entry doesn't appear in the trainer log. Filed as Finding 3 below.
- 23:03 — Per operator: keep run going, do not abort. None of the findings are §4.4 abort conditions; the loop is genuinely working and we want to see the full run wrap (promotion gate against v3, return-to-idle, next-check scheduling).
- 23:07 — Operator screenshot of Improvements tab: "No promotion or rollback events yet" (correct), Reward Trends header reads "Scanned 8 games" (trainer has played ~40 by this time). **Second piece of evidence for Finding 2** — the reward aggregator under-counts trainer games just like Model Comparison does, suggesting the same root cause: trainer-played games skip the logging path the dashboard aggregators read from.
- 23:07 — Cycle 4 done at 23:05:47, win_rate=0.167 → checkpoint v4. Degradation trajectory now 0.333 → 0.250 → 0.200 → 0.167. Cycle 5 in flight. Daemon `state=training`, no errors. 5 checkpoint entries in `/api/training/checkpoints` (v1 dup still present). `trans=1421`.
- 23:09 — Operator screenshot of Decisions tab: "No decisions recorded." Investigation: `GET /api/decision-log` returns `{"entries": []}` because it reads `data/decision_audit.json` ([api.py:319-326](../../src/alpha4gate/api.py#L319-L326)) — and **grep across `src/` shows no code writes that file**. The `/ws/decisions` WebSocket endpoint subscribes clients to `broadcast_decision()` ([web_socket.py:52](../../src/alpha4gate/web_socket.py#L52)) — and **no code calls `broadcast_decision()` anywhere**. The Decision Log feature is dead on arrival across all game modes, not just training. Even the seed game (which printed many `claude_advisor INFO Advisor: response received` lines to stdout) did NOT populate the audit log — those went only through the Python logger, never through the audit pipeline. Filed as Finding 4 below.
- 23:11 — Operator screenshot of Build Orders tab: "No build orders defined." Confirmed working as designed: Build Orders are user-curated via the dashboard editor (full GET/POST/DELETE CRUD in `api.py`), and we started Option B with a fresh `data/` so `data/build_orders.json` doesn't exist. Per soak-test.md §4.1 this tab is in the watch list as "Do not edit during a soak / Ignore." NOT a finding.
- 23:12 — Operator screenshot of Stats tab: "Wins: 1 / Losses: 0", "Simple64: 1W / 0L", "Recent Games: 1 row (the seed game from 22:46)". The trainer has played ~50 games by now but Stats shows 1. Investigation: `/api/stats` reads `data/stats.json` ([api.py:229-232](../../src/alpha4gate/api.py#L229-L232)). `save_stats()` is only called from `runner.py:391` in the `_run_batch` path — **the trainer never writes to stats.json**. There is even an explicit comment at [promotion.py:52](../../src/alpha4gate/learning/promotion.py#L52) reading `"stats.json is NOT consulted."` confirming this is an intentional architectural split. **This consolidates the pattern across Findings 2/4 + Stats: trainer games bypass every legacy dashboard aggregator (stats.json, Model Comparison, Reward Trends) and the Decision Log has no producer at all.** The training-loop-aware surfaces (Loop tab `transitions_since_last`, `/api/training/checkpoints`) are accurate; the legacy surfaces were never re-wired when PPO was added. Promoting Finding 2 to a consolidated architectural finding.
- 23:14 — Cycle 5 done at 23:13:40 with win_rate=0.286 (REBOUND from 0.167). Trajectory: 0.333 → 0.250 → 0.200 → 0.167 → **0.286**. Not strictly monotonic — Finding 1 will be softened from "degrading" to "noisy and well below threshold." Daemon stayed in `state=training` after cycle 5 because the post-cycle evaluator started 20 eval games against v5 at 23:13:40 (`Eval game 1/20 (checkpoint=v5, difficulty=1)`). That's why the run isn't complete yet.
- 23:14 — Operator screenshot of Alerts tab: "Backend errors (3) — alpha4gate.learning.environment: Game thread crashed @ 11:14:03 PM". Investigation: 2 new ERROR-level records (in addition to the synthetic preflight). Backend log lines 3228 and 3268 captured the full sequence: (a) `23:13:26 Timeout waiting for observation from game thread`, then (b) `23:13:40 Game thread crashed` with traceback `sqlite3.IntegrityError: UNIQUE constraint failed: games.game_id` originating in `database.py:194` `store_game()` called from `environment.py:282` `_sync_game()`. **Concrete concurrency / ID-generation bug.** The error was caught and swallowed somewhere up the call stack — cycle 5 immediately continued and reported `win_rate=0.29`, saved checkpoint v5, and entered the promotion path as if nothing happened. This is the exact silent-exception-fallthrough pattern documented in `feedback_silent_exception_fallthrough.md`. Filed as Finding 5 below.
- 23:14 — Backend log line 3298: `2026-04-10 23:13:40,375 alpha4gate.learning.promotion INFO No current best checkpoint -- promoting v5`. The promotion gate auto-promoted v5 with win_rate=0.286 because there was no prior best — first-promotion logic bypasses `win_rate_threshold=0.8`. **Reasonable design** but invisible to the operator. Worse: `/api/training/promotions` returns `[]` even after this log line, so the dashboard Improvements tab will not surface the v5 promotion. Either persistence happens later (after evaluator finishes) or it's a wiring gap. Filed as Finding 6.
- 23:14 — Watchdog (#73) status: error count is 3 total (1 synthetic + 2 real); threshold is 5. Watchdog correctly did NOT fire. But if the `IntegrityError` recurs on subsequent cycles we'll cross the threshold quickly. Daemon `state=training`, `last_error=None`, `runs_completed=0` — orchestrator is mid-evaluator, run is not yet complete.
- 23:20 — Operator screenshot of Replays tab: only 1 entry (`game_Simple64.SC2Replay`) after 50+ trainer games. Investigation: `connection.py:59` builds the replay path as `f"game_{map_name}.SC2Replay"` — a constant filename per map, no game_id or timestamp. Predicted overwrite. But mtime check is decisive: `replays/game_Simple64.SC2Replay` mtime is `Apr 10 22:46` (the SEED game time), now is 23:20. **The trainer isn't writing replays at all.** Confirmed via grep: `learning/environment.py` has zero references to `replay` / `save_replay`, and `from alpha4gate.connection import run_bot` (the only path that calls `save_replay`) is imported only by `runner.py` in the batch / single-game paths. The trainer's environment uses a separate SC2 connection path that bypasses `connection.py:save_replay()` entirely. **Fifth distinct dashboard surface bypassed by the trainer**, joining Stats / Decisions / Model Comparison / Reward Trends. Adding as a new evidence row to Finding 2 (the consolidated architectural finding).
- 23:20 — Side observation while investigating: there are TWO `replays/` directories on disk — `Alpha4Gate/replays/` (canonical, used by current run) and `Alpha4Gate/src/replays/` (stale, mtime Mar 29 17:14). Cause: `_replay_dir = Path("replays")` in `api.py:44` is a relative path that resolves against the runner's cwd. A prior invocation must have launched from `src/`, creating a duplicate replay tree. Tiny separate finding — added below.
- 23:25 — Operator screenshot of Live tab during a running training game: shows `Live Game — 2:23, Step: 3212, 21 probes, defend state, 2 Pylons, 2 Assimilators, 2 Gateways, 1 Nexus`, all matching the SC2 window on the left. **Live tab DOES see the trainer's games** — important counter-evidence to Finding 2. Investigation: the Live tab uses `/ws/game` which is fed by a periodic drain task at [api.py:93-100](../../src/alpha4gate/api.py#L93-L100) that pulls from `drain_broadcast_queue()` and broadcasts via `ws_manager.broadcast_game_state()`. The bot pushes snapshots into the shared queue at the **bot layer**, which both rule-based bots and `_FullTrainingBot` inherit from — so both flows populate the queue. Refines Finding 2: the architectural boundary is "trainer bypasses every producer above the bot layer (connection.py, batch_runner.py, runner.py callbacks) but does NOT bypass bot-layer broadcast queues."
- 23:25 — Operator did NOT exercise the Command Panel (right-hand pane). It's an open question whether sending an AI-Assisted command during a PPO training game has any effect — the trainer's PPO loop is choosing actions, so commands routed through the rule-based decision engine likely have no effect. Worth testing post-soak but not investigating mid-run.
- 23:30 — `error_count_since_start` ticked from 3 → **4**. The 4th error is another `Timeout waiting for observation from game thread` at `23:19:53`, the exact moment eval game 2 started. Pattern: every eval game is hanging at the timeout limit (~6 minutes) and being abandoned, with one ERROR per timeout. **Watchdog (#73) is now one error away from firing.** Predicted next ~10 min: eval game 3 timeout would fire #5 → watchdog trips → daemon writes interim `last_error="Watchdog: 5 ERROR-level log records observed since training started"` → §4.4 abort condition.
- 23:31 — Operator decision to **stop the run early** rather than wait for the watchdog. Rationale: continuing gives diminishing evidence value (we already have 6 concrete findings, several with clean root causes), and the IntegrityError in Finding 5 will compound across more cycles. Better use of time: turn the findings into a build-phase work plan and run autonomously overnight.
- 23:31 — §5.1 stop sequence executed: (A) `POST /api/training/stop` returned `{"status":"stopped"}`, daemon flipped to `state=idle running=False`. (C) `cmd.exe //c "taskkill /T /F /PID 124992"` killed the backend process tree (SUCCESS). PowerShell `Get-Process SC2_x64` showed one survivor: PID 123600 with StartTime 21:07:34, which predates the soak — that's the operator's manually-launched main-menu SC2 client. Per §5.1 it was deliberately NOT killed. Frontend (vite PID 97816) and poller background tasks stopped via TaskStop + taskkill.
- 23:32 — Artifacts captured to `~/soak-artifacts/2026-04-11/`: `training.db` (286 KB), `data-snapshot.zip` (2.4 MB full data/ dir), `backend.log` (404 KB), `frontend.log` (82 KB), `poller.log` (4 KB), `seed.log` (38 KB), `daemon-state.jsonl` (57 KB / 31 polls). Final daemon state via last poll row: `state=idle runs=0 trans=1750 cps=6 err=None`.

## Run summary

**Outcome:** Stopped early at T0 + 35 minutes (~23:31 local) before the §4.4 watchdog abort would have fired. **Did not complete the first orchestrator run** — `runs_completed` finished at 0. Stopped during eval game 2 of 20 of v5's post-cycle promotion evaluation.

**Loop activity observed:** 5 PPO training cycles, ~50 trainer games, 6 checkpoint entries (v1 dup + v1 / v2 / v3 / v4 / v5), 1 auto-promotion of v5 as the first baseline (logged but not in `/api/training/promotions`), 1750 transitions accumulated in `training.db`. Daemon health remained nominal throughout — no real `last_error`, watchdog never tripped (count maxed at 4 of 5 threshold).

**Run is a clear success in evidence value.** 6 concrete findings filed with reproductions and stack-trace-level diagnoses. The autonomous loop architecture works at the daemon-orchestration level (#72 / #73 fixes held); the failures are concentrated in (a) trainer→legacy producer wiring gaps, (b) one concrete sqlite IntegrityError in `database.py:194`, (c) PPO learning-quality issues that are research-scope. The next run, after the build-phase fixes land, should be much more informative.

**Next:** `documentation/plans/soak-2026-04-11-fixes.md` plan doc to be drafted, then `/repo-sync` + `/build-phase` overnight against the Finding 5 / 2 / 4 / 6 / replay-filename / headless-SC2 cluster. Finding 1 (PPO win-rate quality) is research-scope and deferred.

## Alerts fired

| Timestamp | Severity | Rule | Accurate? (y/n/unknown) | Notes |
|---|---|---|---|---|

## Daemon state transitions

| Timestamp | State | Notes |
|---|---|---|

## Findings

| Description | Severity | Category | Evidence |
|---|---|---|---|
| **PPO win rate is noisy and well below the 0.8 promotion threshold at difficulty 1.** Trajectory across 5 cycles: 0.333 → 0.250 → 0.200 → 0.167 → 0.286 (not strictly monotonic but never near promotion). Possible causes (for triage): reward signal mis-shaped, per-cycle experience buffer too small (~100 transitions/cycle), PPO hyperparameters (lr/batch), reward-rule interactions. The autonomous loop will only ever promote via the "no current best -- promote first" path (Finding 6), never via the win-rate gate. | **major** | training | Backend log cycle markers 22:56:04 → 23:13:40; `/api/training/checkpoints` snapshot; 23:03 / 23:07 / 23:14 Timeline entries |
| **Decision Log feature is dead on arrival.** `GET /api/decision-log` reads `data/decision_audit.json` but no code in `src/` writes that file. `/ws/decisions` broadcasts via `broadcast_decision()` but no code calls it. Dashboard Decisions tab is permanently empty across all game modes (rule-based, Claude-advised, PPO trainer). Even the seed game's `claude_advisor INFO Advisor: response received` log lines never reach the audit pipeline — they're pure Python logging output. | **major** | dashboard | `grep -rn 'decision_audit\|broadcast_decision' src/` returns only the consumer sites, no producers; 23:09 Timeline entry |
| **Trainer games bypass every producer above the bot layer.** The architectural boundary is precise: the bot itself broadcasts game-state snapshots into a shared queue ([api.py:93-100](../../src/alpha4gate/api.py#L93-L100) `_drain_and_broadcast_once`) that both rule-based bots and `_FullTrainingBot` inherit, so the **Live tab DOES see the trainer's games**. But the trainer's `learning/environment.py` runs SC2 through its own connection path and does not invoke `connection.run_bot`, so every per-game producer that lives one layer up is bypassed: `save_replay()` (Replays tab), `save_stats()` (Stats tab), Model Comparison aggregator, Reward Trends aggregator. Decisions is a separate dead-on-arrival case — `broadcast_decision()` and `decision_audit.json` have zero producers anywhere in `src/`, so Decisions is dead even for rule-based games (Finding 4). After 50+ trainer games: Stats tab=1 (seed only), Model Comparison=4, Reward Trends=8, Replays=1 (mtime=seed time, not overwritten — simply not written), Decisions=0; Live tab=accurate, Loop tab=accurate, `/api/training/checkpoints`=accurate. Intentional architectural split per [promotion.py:52](../../src/alpha4gate/learning/promotion.py#L52) `"stats.json is NOT consulted."`. Fix options: (a) wire the trainer's per-game lifecycle through the legacy producers, (b) hoist the producers to the bot layer alongside the broadcast queue, or (c) clearly label the legacy surfaces as "manual batch runs only" in the UI. Currently the dashboard silently lies during a soak. | **major** | dashboard / training | Operator screenshots at 23:03, 23:07, 23:12, 23:20, 23:25; `data/stats.json` contents; replay file mtime 22:46 vs current 23:25; `grep -rn save_stats\|save_replay\|broadcast_decision src/` shows only legacy callsites; api.py:93-100 broadcast drain task; promotion.py:52 architectural comment; Timeline entries 23:03 / 23:07 / 23:12 / 23:20 / 23:25 |
| `/api/training/checkpoints` returns two entries named `v1` with the same `cycle=1` but different `win_rate` (0.500 vs 0.333). The 0.500 value does not appear in the trainer log for cycle 1. Either an in-flight intermediate snapshot got cached or a stale-write/dedup bug. | minor | training | `/api/training/checkpoints` snapshot at 23:03; 23:03 Timeline entry |
| soak-test.md §3.2 hard pre-flight requires backend log >50 lines within 30s, but current logging emits only ~7 banner lines (warning + daemon-started + uvicorn). Gate is structurally unmeetable; intent (detect broken tee) is met by content-based check instead. | minor | docs | `logs/soak-2026-04-11-backend.log` (7 lines, healthy content); 22:48 Timeline entry |
| Loop tab "Next Check" timestamp does not refresh while daemon is in `state: training` — it stays pinned at the wakeup time that fired the current run (`10:55:00 PM` in the T0 screenshot, which was already in the past). Cosmetic but potentially confusing during long cycles. | cosmetic | dashboard | T0 dashboard screenshot |
| **`sqlite3.IntegrityError: UNIQUE constraint failed: games.game_id` in trainer game recording, silently swallowed.** Reproduced once during cycle 5 game 10 (or retry-of-10 after a prior `Timeout waiting for observation from game thread` 14s earlier). Stack: `environment.py:282 _sync_game` → `database.py:194 store_game` → INSERT collision. The error was raised in the game thread, the cycle reported `win_rate=0.29` and saved `v5` as if nothing happened — the silent-exception-fallthrough pattern from `feedback_silent_exception_fallthrough.md`. Concurrency / ID-generation bug; likely the trainer's `_game_id` allocation collides after a timeout-induced retry while the previous row is still in WAL. The win_rate computation either includes ghost data from the partial game or silently excludes it. **If this recurs on subsequent cycles, the watchdog (#73) will cross its 5-error threshold quickly.** | **major** (candidate blocker) | training / database | Backend log lines 3228 (timeout), 3268-3293 (crash + traceback), 3294 (cycle continued); `/api/training/status` `recent_errors[2]` and `[3]`; 23:14 Timeline entry |
| Promotion gate auto-promotes the first checkpoint regardless of `win_rate_threshold=0.8` when there is no current best (`No current best checkpoint -- promoting v5` log line at 23:13:40). This is reasonable design (need a baseline) but invisible to the operator and not labeled in the dashboard. Worse: `/api/training/promotions` returns `[]` after this log line, so the Improvements tab will likely not surface the v5 promotion at all. Either persistence happens after evaluator completes, or another aggregator wiring gap. Needs investigation post-soak. | minor (label) + minor (wiring) | dashboard / promotion | Backend log line 3298; `/api/training/promotions` snapshot at 23:14; 23:14 Timeline entry |
| Two `replays/` directories exist on disk: canonical `Alpha4Gate/replays/` and stale `Alpha4Gate/src/replays/` (mtime Mar 29). Cause: `_replay_dir = Path("replays")` ([api.py:44](../../src/alpha4gate/api.py#L44)) is a relative path that resolves against the runner's cwd, so a prior invocation from `src/` created a parallel replay tree. Either pin to an absolute path under `settings.data_dir.parent` or refuse to start when launched from a non-root cwd. | cosmetic | runner / config | `find ... -name '*.SC2Replay'` output; 23:20 Timeline entry |
